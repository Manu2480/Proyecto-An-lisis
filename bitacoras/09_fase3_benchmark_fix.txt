BITÁCORA 09 — Benchmark en ejecución + fix timeout Windows
Fecha: 16/05/2026
Fase: 3 (continuación)

ACCIONES:
  Primera ejecución del benchmark (timeout 300s, sin timeout real en Windows):
  - Los casos de subsistema 9-10 nodos tardaban ~11 min. Sin SIGALRM en Windows.
  - Se mató el proceso manualmente.

  Segunda ejecución (timeout 60s, con threading-based timeout):
  - Se reimplementó time_limit() usando threading.Thread + join(timeout=60)
  - Funciona en Windows: los casos que exceden 60s se marcan con "T" (TIMEOUT)
  - Los casos menores (3-7 nodos de subsistema) convergen normalmente
  - Ejemplo: caso 9 (ABCDEFGHI/ABCDEFGHI) Geo=. (converge en ~13s)
  - Ejemplo: caso 10 (ABCDEFGHI/BCDEFGHIJ) Geo=T (timeout >60s)

ARCHIVOS MODIFICADOS:
  GeoMIP/src/benchmark.py:
    - Eliminada la implementación de time_limit() con @contextmanager
    - Añadida función _run_with_timeout() usando threading.Thread
    - run_strategy() usa _run_with_timeout() en vez de @contextmanager
    - Constructor llamado con clave correcta 'gestor' (no 'manager')

BENCHMARK EN CURSO:
  Archivo de salida esperado: GeoMIP/results/benchmark_2026-05-16_18h*.xlsx
  n=10, 49 casos × 3 estrategias (Geo k=2, QN k=2, KPart k=3)
  Tiempo estimado total: ~2h (casos 1-7 ~5s cada uno, casos 8+ timeout)

INTERPRETACIÓN:
  - Geo=. → GeometricSIA convergió dentro de 60s
  - Geo=T → TIMEOUT (subsistema demasiado grande para un core)
  - QN=T  → QNodes TIMEOUT (sorprendente, podría ser overhead de profiler)
  - KP3=. → KPartitionSIA k=3 convergió (usa subsistemas de la bipartición MIP)
